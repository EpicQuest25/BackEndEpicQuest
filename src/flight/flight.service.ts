import { Injectable } from '@nestjs/common';

@Injectable()
export class FlightService {
  // This service is currently empty but can be extended with flight-related functionality
  // that doesn't rely on the Amadeus API
}